# requirements.txt — Qwen3.6-27B quantization pipeline
#
# Install PyTorch FIRST (see requirements-torch.txt), then:
#   pip install -r requirements.txt

# Hugging Face Ecosystem
transformers==5.6.0          # 5.6.x required; patches in quantize.sh target this version
accelerate==1.12.0
datasets==2.19.0
huggingface-hub==0.36.2
tokenizers==0.22.2
safetensors==0.7.0

# Quantization
llmcompressor==0.10.0.1
compressed-tensors==0.14.0.1

# Utilities
sentencepiece==0.2.1
numpy==2.2.6
tqdm==4.67.3
pyyaml>=6.0.2               # needed for configs/schemes.yaml
